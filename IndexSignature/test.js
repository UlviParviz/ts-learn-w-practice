const stringIndexStringArray = {
    name: "Semistan",
    surname: "Abuzer",
};
const stringArray = ["abc", "qwer"];
const numberArray = [1, 2, 3];
console.log("-----------------------------------------------------");
const circle = { color: "red", radius: 15 };
const { color, radius } = circle;
console.log(`Color: ${color}, Radius: ${radius}`);
